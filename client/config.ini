[server]
; IP atau hostname server/operator
host = 127.0.0.1
; Port TCP server
port = 9999

[client]
; Nama PC ini (tampil di dashboard operator)
pc_name = PC-01
; Interval reconnect otomatis jika putus (detik)
reconnect_interval = 5
